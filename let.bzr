fn factorial(x int) int {
    if x <= 1 {
        ret 1;
    } else {
        ret x * factorial(x - 1);
    }
}

let result = factorial(4);

if result != 24 {
    puts("Feelsbad, result is ", result);
} else {
    puts("Feelsgood, result is ", result);
}